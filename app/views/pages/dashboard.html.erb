<h1 style="font-size: 3rem; font-weight: bold; text-align: center; margin-top: 50px; margin-bottom: 20px;">Dashboard</h1>
<p style="text-align: center; font-size: 1.25rem; color: #555; margin-bottom: 40px;">Welcome, <%= current_user.email %>!</p>


<div class="container">
  <div class="row">
    <% if current_user.owner? %>
      <!-- Owner's Spaces Section -->
      <div class="col-md-6">
        <div class="section-container p-3 mb-4">
          <h2 class="text-center">Your Places</h2>
          <div class="row">
            <% current_user.spaces.each do |space| %>
              <div class="col-md-4 mb-4">
                <div class="card h-100 d-flex flex-column">
                  <img class="card-img-top" src="https://a0.muscache.com/im/pictures/miso/Hosting-47118372/original/7cb880ec-a5f7-4131-9174-d4e1b124bb67.jpeg?im_w=720&im_format=avif" alt="Space image">
                  <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><%= space.name %></h5>
                    <p class="card-text"><%= space.location %></p>
                    <a href="<%= space_path(space) %>" class="btn btn-primary mt-auto">View Details</a>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
        <a href="<%= new_space_path %>" class="btn btn-primary">Add new space</a>
      </div>

      <!-- Owner's Bookings Section -->
      <div class="col-md-6">
        <div class="section-container p-3 mb-4">
          <h2 class="text-center">Your Bookings</h2>
          <div class="row">
            <% current_user.bookings.each do |booking| %>
              <div class="col-md-4 mb-4">
                <div class="card h-100 d-flex flex-column">
                  <img class="card-img-top" src="https://a0.muscache.com/im/pictures/miso/Hosting-47118372/original/7cb880ec-a5f7-4131-9174-d4e1b124bb67.jpeg?im_w=720&im_format=avif" alt="Space image">
                  <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><%= booking.space.name %></h5>
                    <p class="card-text"><%= booking.renter.email %></p>
                    <p class="card-text"><%= booking.status %></p>

                    <% if booking.status == 'pending' %>
                      <div style="display: flex; justify-content: space-between;">
                        <%= button_to accept_booking_path(booking), method: :patch, class: "btn-accept", style: "background-color: transparent; border: none; font-size: 2rem; cursor: pointer; color: #4CAF50;" do %>
                        <i class="fas fa-check-circle"></i>
                        <% end %>
                        <%= button_to decline_booking_path(booking), method: :patch, class: "btn-decline", style: "background-color: transparent; border: none; font-size: 2rem; cursor: pointer; color: #f44336;" do %>
                        &#x274C;
                        <% end %>
                      </div>
                    <% end %>
                    <% if booking.space.owner == current_user %>
                      <%= button_to 'Delete Booking', booking_path(booking), method: :delete, data: { confirm: 'Are you sure you want to delete this booking?' }, style: "background-color: #f0f0f0; border: none; color: #d9534f; font-weight: normal; padding: 10px 20px;" %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

    <% elsif current_user.renter? %>
      <!-- Renter's Bookings Section -->
      <div class="col-md-12">
        <div class="section-container p-3 mb-4">
          <h2 class="text-center">Your Bookings</h2>
          <div class="row">
            <% current_user.bookings.each do |booking| %>
              <div class="col-md-4 mb-4">
                <div class="card h-100 d-flex flex-column">
                  <img class="card-img-top" src="https://a0.muscache.com/im/pictures/miso/Hosting-47118372/original/7cb880ec-a5f7-4131-9174-d4e1b124bb67.jpeg?im_w=720&im_format=avif" alt="Space image">
                  <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><%= booking.space.name %></h5>
                    <p class="card-text"><%= booking.status %></p>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
